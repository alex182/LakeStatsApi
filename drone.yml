kind: pipeline
type: docker
name: Lake_Stats_API

steps:
- name: greeting
  image: alpine
  commands:
  - echo hello world API
- name: docker build
  image: plugins/docker
  command:
    #- echo {"insecure-registries" : [ "192.168.1.136:5000" ]} > your/file.txt
  settings:
    repo: 192.168.1.136:9005/lake_stats_api
    tags: latest
    insecure: false
    build_args:
        - INFLUX_KEY_ARG=$influxKey
    #secret: id=mysecret,src=secret-file
    influxKey:
      from_secret: influxKey
